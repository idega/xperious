<?xml version="1.0" encoding="utf-8"?>
<urlrewrite>

      <!-- This is ok for the prototype. Restrict origin later -->
      <rule>
          <from>^/api/.*$</from>
          <set type="response-header" name="Access-Control-Allow-Origin">*</set>
      </rule>

</urlrewrite>
